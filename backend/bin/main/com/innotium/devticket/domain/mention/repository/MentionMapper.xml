<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.innotium.devticket.domain.mention.repository.MentionRepository">

  <!-- 멘션 저장 -->
  <insert id="insertMention">
    INSERT INTO dts_mentions (ticket_id, source_type, source_id, mentioned_user_id, mentioned_by)
    VALUES (#{ticketId}, #{sourceType}, #{sourceId}, #{mentionedUserId}, #{mentionedBy})
  </insert>

  <!-- 특정 source의 기존 멘션 삭제 (수정 시 재파싱) -->
  <delete id="deleteBySource">
    DELETE FROM dts_mentions
    WHERE ticket_id  = #{ticketId}
      AND source_type = #{sourceType}
      AND (
        (#{sourceId} IS NULL AND source_id IS NULL)
        OR source_id = #{sourceId}
      )
  </delete>

</mapper>
